# Rick's Basement Backend

Use when editing the FastAPI app, SQLite persistence, WebSockets, event processing, session replay, or API schemas.

Rules:

- Privacy mode defaults to `minimal`.
- Redact before storing or broadcasting.
- Keep events small and structured.
- Add tests for normalization, session grouping, and privacy changes.
- Preserve localhost-only CORS defaults.
